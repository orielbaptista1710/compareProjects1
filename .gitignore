# Ignore node_modules in the frontend and backend
node_modules/

frontend/node_modules/
backend/node_modules/
property-validator/node_modules/

# Ignore environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

.env.*    

# ── OS / editor ────── Remove if have vscode extensition or configs u want to share 
.vscode/

#ignore validation files
property-validator/data

# ── Property data CSVs  ───────────────────────────────────────
# Validate locally, then upload directly to MongoDB Atlas or your import pipeline.
data/
*.csv
 
# ── JSON data files ───────────────────────────────────────────
# Same reason — raw imported data should not live in version control.
property-validator/propertySchema.js
*.data.json

#load it via an environment variable or a secrets manager (Railway secrets, AWS Secrets Manager, Doppler, etc.).
# Ignore Firebase configuration file
backend/config/firebase-service-account.json

# SCRIPTS
backend/scripts/

# Ignore dependencies, testing, production, and misc files in the frontend
# frontend specific
/pnp
.pnp.js

# testing
/coverage

# production
/dist 

# misc
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*
